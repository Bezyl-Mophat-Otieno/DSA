let username = "Joe Bidden";

const myName = (name) => {
  let username = name;
};

myName("Bezyl Mophat Otieno");

const fullname = (firstname, lastname) => {
  lastname = lastname || "Mophat";
  const fullname = `${firstname} ${lastname}`;
};

fullname("Bezyl", 0);


